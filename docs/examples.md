# Running examples

This repository includes a large set of runnable examples under `examples/`.

## List of examples

See `examples/README.md` for the full list.

## Run an example

From the repository root:

```bash
npm install
node examples/hello-world
```

Some examples may require additional dependencies (listed in the example folder) or external services (e.g. Redis).

